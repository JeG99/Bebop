routine rec_fib;

procedures:

	proc rec_fib(n: int): int {
		instructions:
			if (n == 0) {
				return 0;
			}
			if (n == 1) {
				return 1;
			}
			else {
				return rec_fib(n - 1) + rec_fib(n - 2);
			}
	}

begin: [
    instructions:
        write rec_fib(7);
]
